### Nextjs

```
cd nextjs

yarn   (installing all library needed).

yarn dev 
```

### subgraph

```
cd subgraph

yarn  (installing all library needed).

yarn codegen (generate the necessary TypeScript bindings for your GraphQL schema)

yarn build ( compile the subgraph and prepare it for deployment).

yarn create-local 

yarn deploy-local

```